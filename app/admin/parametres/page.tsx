import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';
export default async function AdminParametres(){const s=await createClient();const {data}=await s.auth.getClaims();if(!data?.claims?.sub)redirect('/connexion');const {data:p}=await s.from('profiles').select('role,full_name').eq('id',data.claims.sub).single();if(!p||p.role!=='admin')redirect('/espace-client');return <main className="min-h-screen bg-[#f8f8f5] px-5 py-8 lg:px-8"><div className="mx-auto max-w-4xl"><Link href="/admin" className="font-bold text-[#0c4f33]">← Administration</Link><h1 className="mt-5 text-3xl font-bold text-[#0c4f33]">Paramètres</h1><section className="mt-7 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-black/5"><h2 className="text-xl font-bold text-[#0c4f33]">Compte administrateur</h2><dl className="mt-5 grid gap-4 sm:grid-cols-2 text-sm"><div><dt className="text-slate-500">Nom</dt><dd className="mt-1 font-semibold">{p.full_name??'—'}</dd></div><div><dt className="text-slate-500">Rôle</dt><dd className="mt-1 font-semibold">{p.role}</dd></div></dl></section><section className="mt-6 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-black/5"><h2 className="text-xl font-bold text-[#0c4f33]">Configuration</h2><p className="mt-2 text-sm text-slate-600">Cette section est volontairement en lecture seule pour cette première version. Les paramètres métier seront branchés après définition des règles administratives d'Arafat Voyage.</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-[#f5eee0] p-4"><b>Storage documents</b><p className="text-sm text-slate-600">Bucket privé registration-documents</p></div><div className="rounded-2xl bg-[#f5eee0] p-4"><b>Accès administration</b><p className="text-sm text-slate-600">Admin uniquement pour les paramètres</p></div></div></section></div></main>}
